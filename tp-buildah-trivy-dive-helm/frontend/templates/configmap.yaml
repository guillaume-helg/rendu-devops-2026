apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "frontend.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "frontend.labels" . | nindent 4 }}
data:
  API_URL: "http://{{ .Release.Name }}-backend:8080/api"
  NEXT_PUBLIC_API_URL: "/api"
  PORT: "3000"
